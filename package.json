{
  "name": "webui1",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @webui1/components",
    "start": "npm run start -w @webui1/server",
    "dev:components": "npm run dev -w @webui1/components",
    "dev:server": "npm run dev -w @webui1/server"
  }
}
